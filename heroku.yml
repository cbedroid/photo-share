setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: django/Dockerfile.stage
release:
  command:
    - ./entrypoint.do.sh
  image: worker
run:
  web: gunicorn photostare.wsgi.application -bind 0.0.0.0:$PORT
