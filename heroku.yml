setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile.stage
    worker: worker/Dockerfile.stage

release:
  command:
    - entrypoint.do.sh
  image: worker
run:
  web: gunicorn photoshare.wsgi.application -bind 0.0.0.0:$PORT
