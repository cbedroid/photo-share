setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: /app/Dockerfile.stage
    worker: worker/app/Dockerfile.stage
release:
  command:
    - entrypoint.do.sh
  image: worker
run:
  web: gunicorn photoshare.wsgi.application -bind 0.0.0.0:$PORT
